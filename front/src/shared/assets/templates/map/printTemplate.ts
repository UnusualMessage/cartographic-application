import { SchemaState } from "../../../misc";

export const printTemplate: SchemaState = {
  view: true,
  layers: true,

  misc: {
    print: true,
  },

  fastControls: {
    zoomOut: true,
    zoomIn: true,
  },
};
