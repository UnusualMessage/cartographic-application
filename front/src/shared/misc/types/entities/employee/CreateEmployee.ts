export interface CreateEmployee {
  firstName: string;
  secondName?: string;
  patronymic?: string;
  phone?: string;
  birthDate?: string;
  driverCard?: string;
  postId: string;
  organizationId: string;
}
